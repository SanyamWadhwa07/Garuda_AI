fastapi>=0.109.0
uvicorn[standard]>=0.28.0
pydantic>=2.9.0
requests>=2.32.0
httpx>=0.27.0
click>=8.1.7
tomli>=2.0.1
tomli-w>=1.0.0
passlib[bcrypt]>=1.7.4
slowapi>=0.1.9
cryptography>=42.0
psutil>=5.9.0
python-multipart>=0.0.9

# Optional — voice STT (install with: pip install garudaai[voice])
# faster-whisper>=1.0.0

# Optional — RAG document search (install with: pip install garudaai[rag])
# chromadb>=0.5.0
# pypdf>=4.0.0

# Optional — big model slow mode (install with: pip install garudaai[airllm])
# airllm>=2.11.0
